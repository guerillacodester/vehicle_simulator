[version]
# Configuration version - must match passenger_modeler.py version
config_version = 1.4.0

[model]
# Statistical model to use for passenger generation
type = poisson
# Alternative: type = gaussian

[plugin_configs]
# Custom plugin configuration files (optional)
# If not specified, defaults to {model_type}_model.json
poisson = custom_poisson.json
gaussian = gaussian_model.json
# Example: poisson = custom_poisson_config.json

[naming_priority]
priority_1 = names
priority_2  = highway

[data]
# GeoJSON data files for model training/generation
amenities = data/barbados/barbados_amenities.geojson
busstops = data/barbados/barbados_busstops.geojson
landuse = data/barbados/barbados_landuse.geojson
highway = data/barbados/barbados_highway.geojson
names = data/barbados/barbados_names.geojson

[output]
# Where to save the generated passenger model
model_file = models/generated/barbados_passenger_model.json
include_statistics = true
include_validation = true

[processing]
# Processing parameters  
# Reduced to 80m to prevent over-aggregation in dense urban areas
walking_distance_meters = 80
generation_interval_seconds = 60
# Restored normal multiplier after fixing base capacity calculations
passenger_demand_multiplier = 1.0

[processing_files]
amenities=data/barbados/barbados_amenities.geojson
busstops=data/barbados/barbados_busstops.geojson
highway=
landuse=data/barbados/barbados_landuse.geojson
names=

[environment]
# Environmental context for the model
region = barbados
timezone = America/Barbados
coordinate_system = WGS84