# =============================================================================
# ArkNet Fleet System - Environment Variables (SECRETS ONLY)
# =============================================================================
# This file contains ONLY sensitive credentials and API tokens.
# DO NOT commit the actual .env file to version control!
#
# Operational configuration (ports, URLs, enable flags) should be in config.ini
# =============================================================================

# =============================================================================
# STRAPI API AUTHENTICATION
# =============================================================================
# API token for authenticating with Strapi (leave empty if not using authentication)
STRAPI_API_TOKEN=

# =============================================================================
# DATABASE CREDENTIALS (if accessing database directly - usually not needed)
# =============================================================================
# Database credentials are typically in arknet-fleet-api/.env for Strapi only
# Python services connect via Strapi API, not directly to database

# =============================================================================
# THIRD-PARTY SERVICE CREDENTIALS
# =============================================================================
# Add other API keys, tokens, or credentials here as needed
# Examples:
# MAPBOX_API_KEY=
# SENDGRID_API_KEY=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
