# =============================================================================
# Vehicle Simulator - Runtime Configuration
# =============================================================================
# DEPRECATED: Most settings moved to database (operational-configurations table)
# 
# Remaining settings here are development/file-system specific only.
# All operational settings (simulation, engine, gps, conductor) are now in:
#   - Database: operational-configurations (section: vehicle_simulator.*)
#   - Seed file: arknet_fleet_manager/operational_config_seed_data.json
#
# TODO: Migrate logging settings to root config.ini and DELETE this file

[logging]
# Logging configuration (development/file-system specific)
# TODO: Move to root config.ini [logging] section
level = INFO
log_file = logs/vehicle_simulator.log
