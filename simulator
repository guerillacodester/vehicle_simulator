#!/usr/bin/env python3
"""Convenience launcher for the clean vehicle simulator package.

Allows running via ./simulator (Unix) or simulator (Windows if assoc.)
Delegates to: python -m world.vehicle_simulator
"""
import runpy
import sys

if __name__ == "__main__":
    sys.exit(runpy.run_module("world.vehicle_simulator", run_name="__main__"))
