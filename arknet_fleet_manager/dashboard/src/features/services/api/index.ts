// Feature API barrel for Services
// Export thin wrappers that call the global api client, e.g.:
// export * from './serviceApi';

// Ensure this file is treated as a module by TypeScript even when empty
export {};
