{
  "name": "allow-custom-files",
  "version": "1.0.0",
  "description": "Custom admin plugin to allow additional file types (e.g., GeoJSON, GTFS, SHP).",
  "strapi": {
    "name": "allow-custom-files",
    "displayName": "Allow Custom Files",
    "description": "Enable GeoJSON and custom file uploads",
    "kind": "plugin"
  },
  "dependencies": {},
  "peerDependencies": {
    "@strapi/strapi": "^5.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
